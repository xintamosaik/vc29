package main

type Route struct {
	Path  string
	Title string
}

var routes = []Route{
	{"home", "Home"},
	{"signals", "Signals"},
	{"drafts", "Drafts"},
	{"intel", "Intel"},

}

templ navigation(active string) {

	<nav class="navigation main">
		for _, route := range routes {
			if route.Path == active {
				@unlink(route.Title)
			} else {
				@link("/"+route.Path, route.Title)
			}
		}
	</nav>
}
